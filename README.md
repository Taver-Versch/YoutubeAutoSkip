# YoutubeAutoSkip
Simple Python app for pressing skip button on bottom left of screen. Specifically made for youtube videos.



# Requirements:
Run this command to get required dependencies.

"pip install -r requirements.txt"